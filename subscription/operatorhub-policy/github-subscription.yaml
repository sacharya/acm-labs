apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: subscription-github
  namespace: operatorhub
spec:
  channel: operatorhub/channel-github
  placement:
    placementRef:
      kind: PlacementRule
      name: dev-placementrule
  packageFilter:
    filterRef:
      name: policies-github-path
